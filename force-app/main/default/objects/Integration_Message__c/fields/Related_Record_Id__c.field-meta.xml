<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Related_Record_Id__c</fullName>
    <externalId>false</externalId>
    <formula>IF( !ISBLANK(Account__c), Account__c,
IF( !ISBLANK( Contact__c ), Contact__c,
IF( !ISBLANK(Address__c), Address__c,
IF( !ISBLANK(Product2__c), Product2__c,
IF( !ISBLANK(Contract__c), Contract__c, &apos;&apos;)
)
)
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Related Record Id</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
